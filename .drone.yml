---
kind: pipeline
name: default

steps:
- name: build Rust version
  image: rust:alpine
  commands:
  - apk add --no-cache musl-dev
  - cargo build --release
  - echo -n "latest" > .tags

- name: publish
  image: plugins/github-release
  settings:
    api_key:
      from_secret: github-token
    files:
      - target/release/lagerist
    checksum:
      - sha256
  when:
    event: tag

- name: set version tag
  image: alpine:latest
  commands:
  - echo -n ",${DRONE_TAG}" >> .tags
  when:
    branch: master
    event: tag

- name: build docker image
  image: plugins/docker
  settings:
    repo: svedrin/lagerist
    username: svedrin
    password:
      from_secret: docker-registry-pw
    cache_from: "svedrin/lagerist:latest"
  when:
    event:
      exclude:
      - pull_request
---
kind: signature
hmac: ec72525c695e60b9e2aef2c41759f4b8ba63a4049737f0db32c63eaf359b53dd

...
