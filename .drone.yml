---
kind: pipeline
name: default

steps:
- name: build Rust version
  image: rust:alpine
  commands:
  - apk add --no-cache musl-dev
  - cargo build --release

- name: publish
  image: plugins/github-release
  settings:
    api_key:
      from_secret: github-token
    files:
      - target/release/lagerist
    checksum:
      - sha256
  when:
    event: tag

- name: build docker image
  image: plugins/docker
  settings:
    repo: svedrin/lagerist
    tags: latest
    username: svedrin
    dockerfile: Dockerfile.rust
    password:
      from_secret: docker-registry-pw
    cache_from: "svedrin/lagerist:latest"
  when:
    branch: master
    event:
      exclude:
      - pull_request
---
kind: signature
hmac: 0331cea4736c0e63a1edd3b224307b8137c10ab78cbd4a43fa166bde7e736b51

...
